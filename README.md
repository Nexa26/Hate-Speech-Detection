# Hate Speech Detection
A simple machine learning based project in python that detects if a given message is hate speech or not using decision tree classifier algorithm.

## Usage
python main.py

wait for a while
once the accuracy is displayed
enter the message for detection 
and the output will be displayed
